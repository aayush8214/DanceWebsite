touch .gitignore	Creates files that you dont want to push

git config --global user.name "Ayush"

git config --global user.email "emailID"

git init		intitialing

git add . 	//Staging area

git commit -m "Initial Commit"

git status   		//Just to see

$ ssh-keygen -t ed25519 -C "your_email@example.com"

tail <path to id_rsa.pub> 	//Add this key to your guthub account

git remote add origin <ssh url.git>		//copy form repo

git push origin master

